# 单词练练后端协作规则

本仓库维护“单词练练”的统一后端，包括 API、微信身份、个人词库、启蒙内容、学习记录、AI、语音、PostgreSQL、Prisma 和部署。

技术基线：Node.js、TypeScript、Fastify、Prisma 7、PostgreSQL。前端仓库是 `D:\Project\english-start`，使用原生微信小程序。

身份模型只有 `User`，一个微信身份对应一个学习用户。启蒙词、造句题和对话题作为共享内容存储；用户导入后只复制个人词库条目，学习记录与用户关联。

安全要求：所有服务端密钥只能来自环境变量；开发模拟登录在生产环境必须禁用；用户语音和学习数据遵循最小化采集和可删除原则；AI 返回必须经过结构校验，不能直接作为受信任数据写入题库。
